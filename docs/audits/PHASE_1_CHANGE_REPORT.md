# Phase 1 — Change Report

**Date:** 2026-07-08
**Scope of this report:** every pending (uncommitted/untracked) item in the working tree, plus independent re-verification of the audit findings (`FLASH_AUDIT_REPORT.md`) that Phase 2 is about to act on.

---

## 0. The "218 pending changes" premise did not hold — confirmed and corrected

Before touching anything, I checked the real state of the repo rather than trusting the "218" figure:

| Check | Result |
|---|---|
| `git status --short` | **2** untracked items, 0 modified, 0 staged |
| `git diff --stat` (unstaged) | empty |
| `git diff --cached --stat` (staged) | empty |
| `git rev-parse HEAD` vs `origin/main` | **identical** (`e05c807`) — 0 ahead, 0 behind |
| `git rev-list --count HEAD` | 189 commits total, repo-wide, ever |

There is no 218-file changeset anywhere in this repository — not in the working tree, not staged, not as commits ahead of the remote, not as the total commit count. I raised this with you directly rather than inventing a plausible-looking 218-file cluster breakdown; you confirmed the figure is stale and told me to proceed against the real state of the repo instead. This report reflects that real state.

---

## 1. Plain-language summary

There are only two things actually sitting uncommitted in this repo right now: the audit report itself, and a leftover test-coverage report folder from running the test suite. Neither is application code, and neither changes how the app behaves. Separately — and this is the part that matters — I independently re-checked the five Critical findings in the existing audit report (`FLASH_AUDIT_REPORT.md`) against the actual current source code and a live GitHub Actions check, rather than assuming the report is still accurate. All five hold up exactly as written: the JWT-in-logs leak, the dead tracking/chat screens, the negative-quantity price manipulation, the unauthenticated driver-location broadcast, and the CI billing lock are all still live in the code today, with matching line numbers. Nothing here is stale. Phase 2 can proceed against this audit's fix backlog as its real, current basis.

---

## 2. The two pending items, explained

| Item | # files | What it is | Status |
|---|---|---|---|
| `FLASH_AUDIT_REPORT.md` | 1 | The full pre-launch audit report (dated today), produced by a prior session's review pass across all three apps. Not yet committed to git. | Complete, internally consistent document. Re-verified below — accurate as of current `HEAD`. |
| `backend/coverage/` | 1 directory (11 files: `index.html`, `lcov.info`, `lcov-report/`, supporting JS/CSS assets) | The HTML/LCOV coverage report generated by a `jest --coverage` run on 2026-07-08 at 01:14. A disposable build artifact, not source. | Working correctly as a build artifact — it's just untracked and not `.gitignore`d, which is a minor housekeeping gap, not a bug. |

**Why they appear as pending:** both are session byproducts — one from writing the audit, one from running its test-coverage command — that were never `git add`ed or added to `.gitignore`. Neither represents in-progress or at-risk work; both are safe to either commit (the report) or delete/ignore (the coverage folder) once you decide which.

**What they actually do right now:**
- `FLASH_AUDIT_REPORT.md` — static documentation, no runtime effect.
- `backend/coverage/` — static HTML report, no runtime effect, regenerated fresh on every `npx jest --coverage` run. Worth adding `coverage/` to `backend/.gitignore` at some point so it stops appearing as untracked noise — flagging this, not doing it, since it's outside Phase 1/2's scope unless you want it folded into the Medium/Low cleanup pass.

**New risk introduced:** none. Neither item is code; neither is reachable at runtime; neither changes auth, data, or money-handling paths.

---

## 3. Independent re-verification of the audit's Critical findings

Since Phase 2 is about to use `FLASH_AUDIT_REPORT.md` as its literal fix list, I re-checked each Critical finding against the actual current source (not just re-reading the report) before treating it as ground truth:

| Finding | Re-checked against | Result |
|---|---|---|
| **C-0** — CI billing lock | Live `gh run list --workflow=ci.yml` / `gh run view` against `github.com/VuyoMakasana/Flash-App`, right now | **Confirmed still active.** Latest run (`28797487615`, triggered by the current `HEAD` commit `e05c807`) shows all three jobs failing in 2-3s with `"The job was not started because your account is locked due to a billing issue."` Identical to the audit's finding. |
| **C-1** — JWTs logged in plaintext | Read `backend/src/server.js:121` directly | **Confirmed.** Line 121 is exactly `app.use(pinoHttp({ logger }));` — no `redact` option. Matches the audit precisely. |
| **C-2** — Dead tracking/chat (AsyncStorage vs SecureStore) | Read `TrackingScreen.js:154`, `ChatScreen.js:44`; grepped both mobile apps for every `AsyncStorage.getItem` call | **Confirmed, and scope-checked.** Both lines read `await AsyncStorage.getItem('FLASH_TOKEN')` verbatim. Repo-wide grep confirms these are the *only* two `AsyncStorage.getItem` calls referencing an auth token anywhere in either app — the other `AsyncStorage.getItem` calls found (`CheckoutScreen.js`, `FlashContext.js`, `backgroundLocationTask.js`) are for checkout drafts, cached user/cart data, and an active-order-id cache, not auth — so there is no additional hidden instance of this bug beyond the two the audit already names. Also confirmed `flash-user-app/services/api.js` exports a working SecureStore-backed `getToken()` (line 31) that these two files should be using instead. |
| **C-3** — Negative-quantity price/stock manipulation | Read `backend/src/models/Order.js:120-168` | **Confirmed, exact line match.** Line 158 is exactly `const qty = parseInt(item.quantity) || 1;` as cited. No sign check exists anywhere before either the stock decrement (`available - parseInt(item.quantity)`) or the subtotal accumulation. A negative quantity passes the out-of-stock check (`available < negative` is false) and then increases stock via subtraction of a negative number. |
| **C-4** — Unauthenticated driver-location broadcast | Read `backend/src/routes/driverRoutes.js:52-58`, `backend/src/models/Driver.js:115-142` | **Confirmed.** The `/location` route (lines 52-58) has `authenticate` + `requireRole("driver")` + `locationLimiter` but no `requireApprovedDriver` — unlike every sibling driver-order route. `Driver.updateLocation()` (line 115) takes `orderId` straight from the caller and broadcasts to `order:<orderId>` with no query checking `driver_id` ownership anywhere in the function body. |

**Conclusion: the audit report is current, not stale.** Every Critical finding matches the live code and live CI state exactly as described, several down to the exact cited line number. There is no drift between when the audit was written and now — which makes sense, since `HEAD` hasn't moved and nothing has been committed since the audit was produced.

---

## 4. What Phase 2 will actually be acting on

Given the above, Phase 2's real scope is: `FLASH_AUDIT_REPORT.md`'s existing fix backlog (§9 of that report), taken as-is, verified fresh above. There is no separate 218-file review to fold in — that premise is retired per your confirmation.

**Awaiting your go-ahead to begin Phase 2**, starting with C-0 (which I'll flag again as something only you can resolve — GitHub account billing) and then C-1 through C-4 in order, each with its own minimal diff and its own before/after proof exactly as the audit's reproduction steps describe.
